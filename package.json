{
  "name": "webhook-quiet-hours",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --config frontend/vite.config.ts",
    "test": "vitest run --config frontend/vitest.config.ts && cargo test && playwright test",
    "test:browser": "playwright test",
    "build": "vite build --config frontend/vite.config.ts",
    "check": "tsc -p frontend/tsconfig.json --noEmit && cargo fmt --check && cargo clippy --all-targets -- -D warnings"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "@types/node": "^24.3.0",
    "axe-core": "4.11.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
